# Xcode
DerivedData/
build/
*.xcuserstate
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# macOS
.DS_Store

# SwiftPM
.build/
.swiftpm/
.spm-cache/

# Peak trading proxy secrets
backend/.env
backend/.env.*
!backend/.env.example
backend/node_modules/
backend/.sessions.json
backend/*.sessions.json
backend/.sessions*.json

# Local Privy / trading secrets (never commit)
Peak/PrivySecrets.local.plist
**/PrivySecrets.local.plist
*.pem
*.p12
*.key

# Sentry auth token (see sentry.properties.example)
sentry.properties

# Local build output (Cursor/Xcode -derivedDataPath)
.derivedData/

# Wrangler's local cache (account id, deploy metadata) — not repo content
.wrangler/
website/.wrangler/
